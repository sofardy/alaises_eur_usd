#!/bin/bash

# .command файл для macOS - запуск двойным кликом
# Автоматически запускает анализатор ликвидности

echo "🎯 Анализатор ликвидности EUR/USD"
echo "================================"

# Переходим в директорию скрипта
cd "$(dirname "$0")"

# Запускаем основной скрипт
echo "🚀 Запуск анализатора ликвидности EUR/USD..."
./run_gui.sh

# Держим окно открытым
echo ""
echo "🔚 Нажмите любую клавишу для закрытия..."
read -n 1
